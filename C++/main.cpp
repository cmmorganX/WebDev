#include <iostream>  //getting the library up and running, using "input output stream"
                    // used for printing and handling user input

int main() {     // int = integer, main is the function like a power button, parenthesis = parameters
    int file_size = 0;     //file_size = variable - adding "= 100" is initializing the variable
    int counter = 0;
    double sales = 9.99;
    std::cout << "file_size";       //cout = "character out" - object will output one or more characters on the screen -- THIS IS A STATEMENT
    return 0;  // 0 tells the operating system that our program is operating - anything over or under 0 will return an error
}  